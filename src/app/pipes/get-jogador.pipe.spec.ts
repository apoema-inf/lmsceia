import { GetJogadorPipe } from './get-jogador.pipe';

describe('GetJogadorPipe', () => {
  it('create an instance', () => {
    const pipe = new GetJogadorPipe();
    expect(pipe).toBeTruthy();
  });
});
